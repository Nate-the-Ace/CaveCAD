# Outstanding beginner dry runs

The plan's per-task gate is three-part: the headless suite, a live GUI
check, and a beginner dry run. The first two can be taken without the
repository owner; the third cannot, and is not claimed as done here.

Each row is a question to answer while using the tool, not a box to
tick. A "no" is a wording or ordering bug, and the fix goes in before
the next task builds on it.

| Task | Committed as | Dry run still owed |
| --- | --- | --- |
| 2 -- staged menu | `6be9fab` | Open the Cave Survey menu cold and read it top to bottom. Does the order match the order you would teach in? Anything you would move? |
| 3 -- Repair Drawing | `8da895e` | Open a cave that needs repair and run **Repair Drawing** once, reading only the dialog. Could a student tell from the three checkbox labels alone what each does and whether it is safe? (The dialog is modal, so it is the one part of this tool no automated check can reach.) |
| 4 -- Callout | `070d537` | Place one typed callout and one floor-elevation callout. Is the two-option dialog answerable by a student who has not met the word LRUD? The dialog is modal, so it is the one part no automated check can reach. |
| 5 -- Cross Section | `da590d6` | Trace one scanned section end to end using only what is on screen. Two questions: was it obvious how to finish, and how to get out without finishing? That is the whole reason the bay panel exists. Then check the placement still feels like yours -- Capture should preview a proposed spot, take Enter, and let you click somewhere else instead. |
| 6 -- Sketch Scans | `42e479a` | Put a scan under the drawing without touching the menu bar, twice: once from the cave's own scans folder, once with **Add a scan from elsewhere...**. Is everything a student needs now on the panel, or did you reach for something no longer there? |
| 7 -- Surface Data | `7a2aee7` | Run **Surface Data** on a drawing with no location set. Does the refusal tell a beginner exactly what to do next, in words they have already met in the menu? Then run it with both boxes ticked on an anchored cave and check imagery and contours both still land as they used to. |
| 8 -- Survey Notebook | `f4b2254` | Correct a trip's date from inside the Notebook (**... > Edit this trip...**). Was it findable without knowing it used to be its own tool? Check the trip count is unchanged afterwards -- the fork-a-duplicate bug is the whole reason this code exists. |
| Symbol Palette | `5797556` | Open **Symbol Palette** and place three symbols: one in the plan, one inside an elevation band, one inside an open section bay. Do the tiles read as pictures of the symbols, or as a wall of grey? Does the layer readout above them tell you where the next click will land, before you press? Then try the aim: click once for a plain drop, and press-drag-release for a flow arrow -- is the difference between the two obvious from doing it, without being told? Then draw one symbol of your own end to end (**New Symbol...**, draw, **Save Symbol**), close CaveCAD, reopen it, and check your symbol is still in the palette and still places. The panel, the previews, the editor tab and the save/delete round trip have been checked live through the MCP bridge (26/26 suites also pass); the click and the aim-and-size drag have since been driven through the action's own handlers against a real drawing too. What NO check has touched is the mouse hardware itself -- does the gesture FEEL right, does the preview keep up, does the cursor readout follow -- and the modal Save Symbol dialog. Also worth judging: (a) is 5 ft the right default SIZE for a symbol on your sheets, and should Scatter Breakdown's foot-wide boulders be sized the same way? (b) a drag overwrites the panel's Size and Angle fields, so the next plain click inherits them -- right, or surprising? |
| Cross Section, Callout, Repair Drawing, Surface Data | `6fc7df1` | Their OK/Cancel buttons threw in this build and were fixed alongside Symbol Palette's. Open each one's dialog and press OK: does the tool actually do the thing? These were not broken by the palette work, but they were fixed by it, and none has been driven end to end since. |
| Shaped Lines in Feature Trace | `af33c39` | Draw a passage's worth of ledges. Two questions: does pointing at the low side after the drag feel like the answer or like an extra click, and does the preview keep up while you move across the line? Then check the tiles: can you tell flowstone from rimstone dam at tile size, and does a plain feature's tile read as that feature? The Cave Lines toolbar is gone -- if you reach for it, say so, the typed commands still work but that is not the same thing. |
| Panel rework (Symbol Palette + Feature Trace) | `4102476` | Everything below was checked through the MCP bridge, not by a hand on a mouse. Fold and unfold a few sections in each panel by clicking the chevron headers: is the target obvious, and does the panel put itself back the way you left it next time you open it? Right-click a header and move a section up and down. Right-click a symbol tile and try **Duplicate as New Symbol...** on a shipped symbol, then **Rename / Recategorise...** on the copy -- both open a modal dialog, which is the one part no automated check can reach. Then draw with Feature Trace: with Interval and Smoothing gone, is one control point per foot with no smoothing right for every wall you trace, or do you miss the boxes? Finally drag a couple of photos onto the Sketch Scans preview: do they land in the trip folder you expected? |
| Feature Trace search + extension | `cb3393d`, this commit | Two things, both driven through the bridge and the action's own commit(), neither through a hand on a mouse. (1) Type into the new search box while tracing: does filtering to one tile help, or does it cost more than scanning thirteen? Are the alias words the ones you would actually type -- what did you type that found nothing? (2) Trace a long wall in several passes without lifting anything but the mouse: does it come out as ONE line, and does the "extended" message read as reassurance or as noise? Then find a corner where two walls meet and start a stroke there deliberately -- it must NOT join. Then hold Shift and continue a wall traced on an earlier day: SHIFT AT THE PRESS IS THE ONE THING NO CHECK HAS TOUCHED, because the modifier is read off a real mouse event and this build's event objects are only ever exercised by hand. If Shift does nothing, that read is the suspect. |
| Shaped lines extend | this commit | Trace a long ledge in three passes. Does it come out as one ledge with the hachures evenly along it, or can you see where the passes met? Does NOT asking for the side again on a continuation feel right, or do you miss the confirmation? Then Ctrl+Z once: spine and ornament must both come back. Shift at the press is unverified here for the same reason as the plain trace -- it is read off a real mouse event. |
| Trip stamping | this commit | Trace a wall and place a symbol in a passage surveyed on a LATER trip than the one next door, then check what each carries: select the line, open its properties and read the CaveSurvey **Trip** value. Two questions no headless check can ask. (1) Is "the trip that reached the nearest station" the answer you would have given for that wall, or is there a wall on your own maps where you would have said a different trip? A wall traced across a junction is the case to look for. (2) Nothing on screen says which trip a stroke was filed under, before or after the drag -- is that right, or would you want the panel to tell you, the way the layer readout does? Saying nothing is the deliberate choice here: a readout invites a caver to trust it, and the answer is derived rather than chosen. |
| Delete Last and Erase | this commit | Both buttons were driven through their own headless suite against a real drawing; what no check has touched is the hand on the mouse. Trace four walls, notice the second one wandered, and clear it with **Erase** -- is one click on the line the gesture you expected, or did you aim at it and miss? Then try to erase something you must not be able to: click the centerline, a station, a scanned page. Nothing happens and the command line says why -- is that reassuring or confusing? Press Escape: the button must un-press itself and tracing must come back armed to the tile you left armed. Then trace a wall in two passes and press **Delete Last**: it refuses, because the second stroke grew the first. Read that refusal cold -- does it send you to Ctrl+Z, or does it read as the button being broken? |
| Keyboard arming | this commit | Trace with one hand on the keyboard: type a few letters into the search box and press Return -- does arming happen where you expected, and is "1 tile left" the right threshold, or would you rather Return took the first match? Try it in both panels. Then press Escape mid-job: the tile must stop looking armed while the feature you were using stays armed for the next drag -- is that the behaviour you want, or should Escape forget the feature too? |
| Shift at the press (still owed, and now known un-automatable) | this commit | Unchanged from the last two commits: hold Shift and continue a wall traced on an earlier day. Worth recording why it is still owed -- synthetic mouse events were tried through the MCP bridge and a single injected QMouseEvent sent to the graphics view takes CaveCAD down (measured twice, with and without one of our actions running). There is no automated route to this; it is a hand on a mouse or nothing. |
| Recent strip | this commit | Draw for half an hour, then look at the Recent strip in both panels. Three questions: is five the right number, or do you only ever reach for three? Are the pictures alone enough to tell them apart at that size -- surveyed walls against breakdown is the hard pair -- or does it need the names back? And does most-recent-FIRST match what your hand expects, or would you rather the row stayed still so a feature keeps its position? |
| Tile counts | this commit | Open a cave you know and read the counts against what you believe is drawn -- do they agree? Two things to judge. (1) A FRESH template drawing already counts 1 for Surveyed Walls and 1 for Inferred Walls: the template carries its legend swatches in model space, on those very layers (measured -- a 3-unit line at 23, -2.2). The count is honest about what is on the layer, but it is not what a caver means by "how much have I drawn". Should the legend move to a layer of its own, or is a permanent 1 acceptable? (2) Is per-VIEW the right scope, or would you rather see the whole cave's total whichever view you are in? |
