{
  "planPath": "docs/superpowers/plans/2026-08-29-section-sketching.md",
  "tasks": [
    {
      "id": 0,
      "subject": "Task 1: CsSectionBay \u2014 the pure maths",
      "status": "completed",
      "description": "**Goal:** Every geometric decision the bay makes \u2014 where it parks, what is inside it, how the scan sits over the ghost, where the finished block lands \u2014 exists as a pure, node-tested function before any of it touches a document.\n\n**Files:**\n- Create: `scripts/CaveSurvey/Core/CsSectionBay.js`\n- Modify: `scripts/CaveSurvey/Core/CsAll.js`\n- Test: `tests/js_unit.js` (append a `CsSectionBay` section; add the file to the CORE list near line 112)\n\n**Acceptance Criteria:**\n- [ ] `CsSectionBay.js` contains no `R*` or `Q*` symbol and runs under node\n- [ ] `frameRectFor` parks the bay clear of the plan extents, and honours a remembered position when given one\n- [ ] `contains` is true only when a box lies wholly inside the rect; a box crossing the edge is out\n- [ ] `sweepOf` returns the ids inside the rect, minus excluded ids, in input order\n- [ ] `serializeFit`/`parseFit` round-trip, and `parseFit` returns `null` on junk rather than throwing\n- [ ] `marchOut` returns the first clear point, `null` when the cap is reached\n- [ ] `clearerSide` returns `+1` or `-1` by obstacle count, `+1` on a tie\n- [ ] `CsSectionBay.js` appears in `CsAll.js`\n\n**Verify:** `node tests/js_unit.js` \u2192 `### UNIT OK <n> assertions`, and `python3 -m unittest tests.test_addon -v` \u2192 `test_every_core_file_is_included_by_csall` passes\n\n**Steps:** (full code in the plan doc) 1) append the CsSectionBay unit tests to tests/js_unit.js and add the file to its CORE list; 2) run node tests/js_unit.js and watch it fail on the missing file; 3) write Core/CsSectionBay.js \u2014 GAP/MARGIN/STEP/CAP constants, frameRectFor, contains, sweepOf, fitTransform, serializeFit, parseFit, overlaps, boxAt, marchOut, perpOf, clearerSide; 4) register it in CsAll.js; 5) run node + test_addon to green; 6) MUTATION TEST: change `contains` to `>` on box.x1, confirm the flush-with-the-frame assertion goes red, revert; 7) commit.\n\n```json:metadata\n{\"files\": [\"scripts/CaveSurvey/Core/CsSectionBay.js\", \"scripts/CaveSurvey/Core/CsAll.js\", \"tests/js_unit.js\"], \"verifyCommand\": \"node tests/js_unit.js && python3 -m unittest tests.test_addon\", \"acceptanceCriteria\": [\"no R* or Q* symbols; runs under node\", \"frameRectFor parks clear of plan extents and honours a remembered corner\", \"contains rejects a box crossing the edge, accepts flush\", \"sweepOf drops excluded ids and keeps input order\", \"serializeFit/parseFit round-trip; parseFit returns null on junk\", \"marchOut returns first clear point, null at the cap\", \"clearerSide ties positive\", \"CsSectionBay.js listed in CsAll.js\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 1,
      "subject": "Task 2: The shelf offers Cross Section",
      "status": "completed",
      "description": "**Goal:** The Sketch Scans shelf's two-state profile checkbox becomes a three-way combo, and a section scan's station list comes from the PLAN's stations \u2014 not from the `SectionStation` tag nothing writes.\n\n**Files:**\n- Modify: `scripts/CaveSurvey/Core/CsScanFrame.js`\n- Modify: `scripts/CaveSurvey/SketchScans/SketchScans.js:538-546` (`frameNow`), plus the widget construction that creates `w.profileCheck`\n- Test: `tests/js_unit.js`\n\n**Acceptance Criteria:**\n- [ ] `CsScanFrame.stationFrameFor(\"section\")` returns `\"plan\"`; `\"profile\"` returns `\"profile\"`; anything unknown returns `\"plan\"`\n- [ ] The shelf shows a combo with Plan / Profile / Cross Section, defaulting to Plan\n- [ ] `frameNow()` returns `\"section\"` when Cross Section is chosen\n- [ ] The station list for a section scan is non-empty on a drawing that has plotted plan stations\n- [ ] Inserting with Cross Section chosen lands the image on `CTRL-SECTION-SCAN` with `ScanFrame=section`\n\n**Verify:** `node tests/js_unit.js` \u2192 `### UNIT OK`, then `./tests/run_all.sh` \u2192 all sections pass\n\n**Steps:** (full code in the plan doc) 1) append the stationFrameFor assertions to the existing CsScanFrame IIFE in tests/js_unit.js; 2) run node and watch it fail with `stationFrameFor is not a function`; 3) add `CsScanFrame.stationFrameFor` after `runTagFor`, documenting why SectionStation must not be read until something writes it; 4) node to green; 5) replace `w.profileCheck` with `w.frameCombo` (QComboBox, three items, index 0 default) and delete the checkbox and its connect; 6) rewrite `frameNow()` to switch on currentIndex; 7) route `stationsNow()` through `CsScanFrame.stationFrameFor(frameNow())` and fix the prompt wording near line 615; 8) ./tests/run_all.sh; 9) commit.\n\n```json:metadata\n{\"files\": [\"scripts/CaveSurvey/Core/CsScanFrame.js\", \"scripts/CaveSurvey/SketchScans/SketchScans.js\", \"tests/js_unit.js\"], \"verifyCommand\": \"node tests/js_unit.js && ./tests/run_all.sh\", \"acceptanceCriteria\": [\"stationFrameFor: section->plan, profile->profile, unknown->plan\", \"shelf shows a three-way combo defaulting to Plan\", \"frameNow returns 'section' on the third entry\", \"section station list is non-empty on a drawing with plotted plan stations\", \"insert lands on CTRL-SECTION-SCAN with ScanFrame=section\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 2,
      "subject": "Task 3: Sketch Section opens the bay",
      "status": "completed",
      "blockedBy": [
        0,
        1
      ],
      "description": "**Goal:** Choosing a scan, a frame of Cross Section and Sketch Section puts a framed bay on the drawing holding that scan and the station's computed LRUD outline, with the view zoomed to it and snapping switched to free.\n\n**Files:**\n- Create: `scripts/CaveSurvey/SketchSection/SketchSection.js`, `scripts/CaveSurvey/SketchSection/SketchSection.svg`\n- Modify: `scripts/CaveSurvey/SketchScans/SketchScans.js` (third action button + `sketchSoon`), `scripts/CaveSurvey/Core/CsCallout.js` (four KEY entries + `SOURCE_SKETCH`)\n\n**Acceptance Criteria:**\n- [ ] `Cave Survey > Sketch Section` appears in the menu and answers to `sketchsection` / `sks`\n- [ ] The shelf's third button is enabled only when the frame combo says Cross Section\n- [ ] The bay frame lands on `CTRL-SECTION-BOX`, locked, clear of the plan extents\n- [ ] The scan lands on `CTRL-SECTION-SCAN`, faded, at the back of the draw order, scaled onto the ghost\n- [ ] The ghost lands on `CTRL-SECTION-OUTLINE`, dashed, at `CsSectionDraw.SCALE`\n- [ ] A station with no cuttable LRUD opens the bay with no ghost and says so, rather than failing\n- [ ] Snap is free while the bay is open and the prior snap is restored on `finishEvent`\n\n**Verify:** `./tests/run_all.sh` \u2192 sections 1 and 2 pass (`### SYNTAX OK`); behavioural proof lands in Task 7\n\n**Steps:** (full code in the plan doc) 1) write SketchSection.js \u2014 run/cutAt/planBoxOf/rememberedCorner/addFrame/addGhost/addScan/zoomTo/snapFree/askStation/init, every layer write wrapped in CsLayers.withLayerOn; 2) add SECTION_SOURCE/SECTION_SCAN/SECTION_FIT/SECTION_STATION to CsCallout.KEY and CsCallout.SOURCE_SKETCH; 3) add the shelf's third button, enable it from the combo, and add `SketchScans.sketchSoon` using the zero-delay QTimer hand-off; 4) draw SketchSection.svg from CrossSection.svg; 5) ./tests/run_all.sh (sort order 47 \u2014 if the structural test reports a collision, take the next free number); 6) commit.\n\n```json:metadata\n{\"files\": [\"scripts/CaveSurvey/SketchSection/SketchSection.js\", \"scripts/CaveSurvey/SketchSection/SketchSection.svg\", \"scripts/CaveSurvey/SketchScans/SketchScans.js\", \"scripts/CaveSurvey/Core/CsCallout.js\"], \"verifyCommand\": \"./tests/run_all.sh\", \"acceptanceCriteria\": [\"menu entry and sketchsection/sks commands exist with a unique sort order\", \"third shelf button enabled only for the section frame\", \"frame on CTRL-SECTION-BOX clear of the plan extents\", \"scan on CTRL-SECTION-SCAN faded and scaled onto the ghost\", \"ghost on CTRL-SECTION-OUTLINE at CsSectionDraw.SCALE\", \"no cuttable LRUD opens the bay without a ghost and explains\", \"snap free while open, restored on finishEvent\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 3,
      "subject": "Task 4: Capture \u2014 sweep, block, leader, placement",
      "status": "completed",
      "blockedBy": [
        0,
        2
      ],
      "description": "**Goal:** Capture turns everything inside the bay into a `CS_<CalloutId>` block, leadered to the station and marched out clear of the plan's walls, with the proposal shown as a preview before it commits.\n\n**Files:**\n- Create: `scripts/CaveSurvey/SketchSection/SectionCapture.js`, `scripts/CaveSurvey/SketchSection/SectionCapture.svg`\n- Test: `tests/section_sketch_run.js` (written in Task 7)\n\n**Acceptance Criteria:**\n- [ ] Capture with nothing traced refuses and explains, rather than defining an empty block\n- [ ] The block definition contains the traced entities and neither the scan, the ghost nor the frame\n- [ ] The block is drawn block-local: the ghost's centre becomes the block origin\n- [ ] The reference carries `SectionSource=sketch`, `SectionScan`, `SectionBayFit`, `SectionStationRef` and `SectionScale`\n- [ ] A leader runs from the station to the section\n- [ ] The proposed position clears every plan-frame obstacle by at least `CsSectionBay.MARGIN`\n- [ ] When the march finds nowhere, the tool falls back to click-to-place instead of committing a bad spot\n- [ ] Bay teardown removes the frame, the ghost, the scan and the loose tracing\n\n**Verify:** `./tests/run_all.sh` \u2192 `### SECTION SKETCH OK` (the test arrives in Task 7; do not claim this verified on syntax alone)\n\n**Steps:** (full code in the plan doc) 1) write SectionCapture.js \u2014 beginEvent/enterEvent/pickCoordinate, findBay (diff the id set, `entity.update()` before every box read), originOf, localBoxOf, obstaclesOf (plan frame only, via CsLayers.frameOf), proposePosition, tangentAt, finish, the static `capture()` so the headless test can drive it without a GUI event, addLeader (STRAIGHT \u2014 a DXF LEADER record has no bulge), init at sort order 48; 2) draw SectionCapture.svg; 3) ./tests/run_all.sh for structure and syntax; 4) commit.\n\n```json:metadata\n{\"files\": [\"scripts/CaveSurvey/SketchSection/SectionCapture.js\", \"scripts/CaveSurvey/SketchSection/SectionCapture.svg\"], \"verifyCommand\": \"./tests/run_all.sh\", \"acceptanceCriteria\": [\"empty bay refuses with an explanation\", \"block holds the tracing and not the scan, ghost or frame\", \"block-local about the ghost centre\", \"reference carries all five tags\", \"a leader runs from the station to the section\", \"proposed position clears obstacles by MARGIN\", \"a failed march falls back to click-to-place\", \"teardown removes frame, ghost, scan and loose tracing\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 4,
      "subject": "Task 5: Draw never regenerates over a sketch",
      "status": "completed",
      "blockedBy": [
        3
      ],
      "description": "**Goal:** `CalloutWrite.refreshSections` skips a `SectionSource=sketch` block and counts it, so hand tracing is never overwritten and never silently stale.\n\n**Files:**\n- Modify: `scripts/CaveSurvey/Callout/CalloutWrite.js:912-940` (`refreshSections`)\n- Test: `tests/cross_section_run.js`\n\n**Acceptance Criteria:**\n- [ ] `refreshSections` returns a `sketched` count alongside `updated`/`unchanged`/`frozen`/`lost`/`refused`\n- [ ] A `SectionSource=sketch` block is left byte-identical after a refresh\n- [ ] A computed section beside it still regenerates in the same pass\n- [ ] A second pass reports the same counts\n\n**Verify:** `/Applications/CaveCAD.app/Contents/MacOS/CaveCAD -no-dock-icon -no-gui -allow-multiple-instances -autostart tests/cross_section_run.js \"$PWD\"` \u2192 `### CROSS SECTION OK`\n\n**Steps:** (full code in the plan doc) 1) append the sketched-section assertions to tests/cross_section_run.js \u2014 build a block, a traced line inside it and a reference tagged SectionSource=sketch, then refresh; 2) run the engine test and watch it fail (`sketched` is undefined); 3) add `sketched: 0` to the report and the skip-and-count check immediately BEFORE the SECTION_FROZEN check; 4) run to green; 5) MUTATION TEST: invert the comparison to `!==`, confirm 'a sketched section is never re-derived' goes red, revert \u2014 this step is not optional; 6) commit.\n\n```json:metadata\n{\"files\": [\"scripts/CaveSurvey/Callout/CalloutWrite.js\", \"tests/cross_section_run.js\"], \"verifyCommand\": \"/Applications/CaveCAD.app/Contents/MacOS/CaveCAD -no-dock-icon -no-gui -allow-multiple-instances -autostart tests/cross_section_run.js \\\"$PWD\\\"\", \"acceptanceCriteria\": [\"report carries a sketched count\", \"a sketched block is unchanged by a refresh\", \"a computed section still regenerates in the same pass\", \"a second pass reports the same counts\", \"the gate has been mutation-tested red then reverted\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 5,
      "subject": "Task 6: Edit Sketch reopens the bay",
      "status": "completed",
      "blockedBy": [
        3
      ],
      "description": "**Goal:** Selecting a sketched section and running Edit Sketch reopens a bay holding that section's linework, its scan at the same fit and the station's ghost \u2014 so a section can be revised against the field book instead of by memory.\n\n**Files:**\n- Create: `scripts/CaveSurvey/SketchSection/SectionEdit.js`, `scripts/CaveSurvey/SketchSection/SectionEdit.svg`\n\n**Acceptance Criteria:**\n- [ ] Running with nothing selected explains what to select, rather than doing nothing\n- [ ] Running on a computed (non-sketch) section explains that only sketched sections reopen\n- [ ] The reopened bay holds the block's entities, exploded back to loose linework at the bay origin\n- [ ] The scan returns at the stored `SectionBayFit`\n- [ ] A missing scan file reopens the bay without an underlay and names the file it could not find\n- [ ] The block reference is deleted on reopen, and re-created by the next Capture \u2014 the section is in exactly one place at a time\n\n**Verify:** `./tests/run_all.sh` \u2192 sections 1 and 2 pass; the round trip is covered by Task 7's `### SECTION SKETCH OK`\n\n**Steps:** (full code in the plan doc) 1) write SectionEdit.js \u2014 run, selectedSection, explodeInto (clone into model space, delete the reference and its leaders in the same operation), bayOriginOf, init at sort order 49, plus the CalloutWrite include; 2) draw SectionEdit.svg; 3) ./tests/run_all.sh; 4) commit.\n\n```json:metadata\n{\"files\": [\"scripts/CaveSurvey/SketchSection/SectionEdit.js\", \"scripts/CaveSurvey/SketchSection/SectionEdit.svg\"], \"verifyCommand\": \"./tests/run_all.sh\", \"acceptanceCriteria\": [\"no selection explains what to select\", \"a computed section explains that only sketched sections reopen\", \"the bay holds the block's entities as loose linework at the bay origin\", \"the scan returns at the stored SectionBayFit\", \"a missing scan reopens without an underlay and names the file\", \"the reference and its leaders are deleted on reopen\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 6,
      "subject": "Task 7: The lifecycle proved on a real document",
      "status": "completed",
      "blockedBy": [
        2,
        3,
        4,
        5
      ],
      "description": "**Goal:** A headless test drives the whole sequence \u2014 open a bay, trace into it, capture, refresh, reopen \u2014 against a real `RDocument`, and `run_all.sh` runs it.\n\n**Files:**\n- Create: `tests/section_sketch_run.js`\n- Modify: `tests/run_all.sh`, `VERSION`, `README.md`, `tests/README.md`\n\n**Acceptance Criteria:**\n- [ ] The test prints `### SECTION SKETCH OK <n>` on success\n- [ ] It proves the sweep excludes the scan, the ghost and the frame\n- [ ] It proves the block is block-local about the ghost centre\n- [ ] It proves the placed reference clears every plan obstacle by the margin\n- [ ] It proves a DXF round trip preserves the block, the reference and all five tags\n- [ ] `run_all.sh` runs it and fails the suite when it fails\n- [ ] `VERSION` reads 0.9.38.0\n\n**Verify:** `./tests/run_all.sh --publish` \u2192 every section passes, then `./tests/run_all.sh; echo \"exit=$?\"` \u2192 `exit=0`\n\n**Steps:** (full skeleton in the plan doc) 1) write tests/section_sketch_run.js following cross_section_run.js's shape \u2014 copy its isNull shim, loadRepoScript and document construction verbatim, and the round-trip helpers from profile_draw_roundtrip.js; the three bracketed passages in the plan MUST be filled with real code and must not survive into the commit; 2) run it and fix the TOOLS, not the test, unless the expectation itself is wrong; 3) add the '8/13' section to run_all.sh and renumber the N/12 headers to N/13; 4) `echo \"0.9.38.0\" > VERSION`; 5) add the three tools to README.md and the test to tests/README.md; 6) ./tests/run_all.sh --publish, then confirm a zero exit status; 7) commit.\n\n```json:metadata\n{\"files\": [\"tests/section_sketch_run.js\", \"tests/run_all.sh\", \"VERSION\", \"README.md\", \"tests/README.md\"], \"verifyCommand\": \"./tests/run_all.sh --publish\", \"acceptanceCriteria\": [\"prints ### SECTION SKETCH OK\", \"proves the sweep excludes scan, ghost and frame\", \"proves the block is block-local about the ghost centre\", \"proves the placed reference clears obstacles by the margin\", \"proves a DXF round trip keeps the block, reference and all five tags\", \"run_all.sh runs it and fails the suite on failure\", \"VERSION reads 0.9.38.0\", \"no bracketed placeholder survives in the committed test\"], \"modelTier\": \"standard\"}\n```"
    }
  ],
  "lastUpdated": "2026-08-30T08:42:45Z"
}